/* LTI lead routing config — fill later, site works without it */
window.LTI_CONFIG = {
  ADMISSIONS_EMAIL: "",        // e.g. "admissions@yourdomain.com" — you will provide
  TELEGRAM_BOT_TOKEN: "",      // e.g. "123456:ABC..." — you will provide
  TELEGRAM_CHAT_ID: "",        // e.g. "-1001234567890" or user id — you will provide
  BACKEND_URL: "",             // recommended: your /api/lead endpoint (Vercel/Apps Script). See README.
  LMS_API_BASE: "https://lti-robotics-lms-backend.onrender.com", // Render FastAPI backend (auth/courses/leads)
  PHONE_DISPLAY: "8610621246",
  PHONE_TEL: "+918610621246"
};
